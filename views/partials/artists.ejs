<div class="container-fluid">
   <div class="row">
     <% artists.forEach(function(artist) { %>
	    <a href="#" onclick="getArtistPage();">
	      <img src="http://<%=root%>/getcover?artist=<%=artist%>" alt="<%=artist%>" class="img-thumbnail">
	   </a>
	 <% } %>
   </div> 
</div>